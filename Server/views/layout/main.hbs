<main class="main">
    <div class="container">
        <div class="content">
        {{#if renderPostsPage}}
            {{> post}}
        {{else}}
            {{> add}}
        {{/if}}
        {{> paginator}}
        </div>
        {{> sidebar}}
    </div>
</main>